package main

/*func main()  {
	f, err := os.OpenFile("test.txt", os.O_RDWR, 0600)
	if err != nil {
		fmt.Println("Open err:", err)
		return
	}
	defer f.Close()

	buf := make([]byte, 4096)	// 4k

	n, err := f.Read(buf)
	if err != nil {
		fmt.Println("Read err:", err)
		return
	}
	fmt.Printf("read data:%s", buf[:n])
}*/

/*func main()  {
	f, err := os.OpenFile("test.txt", os.O_RDWR, 0600)
	if err != nil {
		fmt.Println("Open err:", err)
		return
	}
	defer f.Close()

	n, err := f.Write([]byte("AAA"))
	if err != nil {
		fmt.Println("Write err:", err)
		return
	}
	fmt.Println("n = ", n)
}*/


